On Salary Rules form, one or more tags can be selected and assigne to the rule,
on the "Tags" field.

In salary rules with Python code, totals for a tag can be accessed
using the ``tags`` browseable collection with the uppercase tag name.

Example:
```
result = tags.TAXABLE
```

